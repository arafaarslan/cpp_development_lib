#ifndef _BUBBLE_SORT_H_
#define _BUBBLE_SORT_H_

#include "Sort.h"

class BubbleSort : public Sort {

    public : 
        BubbleSort(int size);
        void doit(int arr[]);
    private :

};

#endif