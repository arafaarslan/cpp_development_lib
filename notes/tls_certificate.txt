# private key
openssl genrsa -out server.key 2048

# self-signed cert (365 gün geçerli)
openssl req -new -x509 -key server.key -out server.crt -days 365 \
  -subj "/C=US/ST=State/L=City/O=Org/OU=Unit/CN=localhost"